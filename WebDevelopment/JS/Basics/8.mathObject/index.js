// Math = built in object that provides a collection of properties and methods for mathematical constants and functions
/*
console.log(Math.PI); // Outputs the value of PI
console.log(Math.E);  // Outputs the value of Euler's number
Math.round(4.7);    // Rounds to the nearest integer, outputs 5
Math.floor(4.7);   // Rounds down, outputs 4
Math.ceil(4.2);    // Rounds up, outputs 5
Math.sqrt(16);    // Square root, outputs 4
Math.abs(-7.5);  // Absolute value, outputs 7.5
Math.pow(2, 3); // Exponentiation, outputs 8
Math.min(3, 1, 4, 2); // Minimum value, outputs 1
Math.max(3, 1, 4, 2); // Maximum value, outputs 4
Math.random(); // Random number between 0 (inclusive) and 1 (exclusive)
Math.trunc(4.9); // Removes decimal part, outputs 4
*/